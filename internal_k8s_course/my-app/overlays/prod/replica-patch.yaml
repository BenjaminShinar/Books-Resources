# my-app/overlays/prod/replica-patch.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
spec:
  replicas: 3          # Prod: 3 replicas for high availability
  template:
    spec:
      containers:
        - name: nginx
          image: nginx:1.25.3    # Prod: pinned to exact version